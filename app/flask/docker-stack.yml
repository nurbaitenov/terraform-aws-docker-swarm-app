version: "3.8"

services:
  flask:
    image: nbdevops3036/flask-app:1.0

    ports:
      - "5000:5000"

    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure

    networks:
      - app

networks:
  app:
    driver: overlay